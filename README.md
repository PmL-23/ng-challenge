A React + TypeScript app that allows candidates to browse open job positions and submit applications with a GitHub repository URL.

## 📦 Installation

Clone the repository:

git clone https://github.com/pml-23/ng-challenge.git

Install dependencies:

npm install

## ▶️ Run the project

npm run dev

## 🔐 Environment Variables

Create a `.env` file in the root directory following the format of the `.env.example` file.